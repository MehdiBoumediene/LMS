{{ form_start(form) }}
<div class="form-row">
	<div class="col-md-2">
		{{ form_row(form.user,{'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.user) }}
	</div>
	<div class="col-md-2">

		{{ form_row(form.chapitre,{'attr': {'class': 'form-control'}}) }}
		{{ form_errors(form.chapitre) }}
	</div>
	<div class="col-md-2">

		{{ form_row(form.heur,{'attr': {'class': 'form-control heur'}}) }}
		{{ form_errors(form.heur) }}
	</div>
	<div class="col-md-2">

		{{ form_row(form.minutes,{'attr': {'class': 'form-control minutes'}}) }}
		{{ form_errors(form.minutes) }}
	</div>

	<div class="col-md-2">
		{{ form_row(form.secondes,{'attr': {'class': 'form-control secondes'}}) }}
		{{ form_errors(form.secondes) }}
	</div>
	<div class="col-md-2">
		{{ form_row(form.timer,{'attr': {'class': 'form-control timer'}}) }}
		{{ form_errors(form.timer) }}
	</div>
</div>
<br>
<button class="mt-1 btn btn-success">{{ button_label|default('Sauvegarder') }}</button>
ou
<a href="{{ path('app_etudiants_index') }}">Retour à la liste</a>


{{ form_end(form) }}


<script src="https://code.jquery.com/jquery-2.2.4.min.js" integrity="sha256-BbhdlvQf/xTY9gja0Dq3HiwQF8LaCRTXxZKRutelT44=" crossorigin="anonymous"></script>
<script>

	$(".heur").on('input', function (e) {
var heur = ($('.heur').val() * 3600000);
var minutes = ($('.minutes').val() * 60000);
var secondes = ($('.secondes').val() * 1000);
var total = (heur + minutes + secondes)

$('.timer').val(total)
});

$(".minutes").on('input', function (e) {
var heur = ($('.heur').val() * 3600000);
var minutes = ($('.minutes').val() * 60000);
var secondes = ($('.secondes').val() * 1000);
var total = (heur + minutes + secondes)
$('.timer').val(total)
});
$(".secondes").change('input', function (e) {
var heur = ($('.heur').val() * 3600000);
var minutes = ($('.minutes').val() * 60000);
var secondes = ($('.secondes').val() * 1000);
var total = (heur + minutes + secondes)
$('.timer').val(total)
});
</script>
