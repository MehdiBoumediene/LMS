
      <form method="POST" action="{{ path('app_modules_new') }}" enctype="multipart/form-data">
   <div class="form-row">
        <div class="col-md-5">
        {{ form_row(form.nom,{'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.nom) }}
        </div>
       <div class="col-md-5">
       <br><br>

        {{ form_row(form.medias,{'attr': {'class': ''}}) }}
        {{ form_errors(form.medias) }}
        </div>
 
        

    </div>

 {{ form_rest(form) }}

    <br>
    <button class="mt-1 btn btn-success">{{ button_label|default('Sauvegarder') }}</button> ou <a href="{{ path('app_modules_index') }}">Retour à la liste</a>
   </form>

